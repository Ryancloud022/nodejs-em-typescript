import { App } from "./app";

const app = new App(); // Cria uma nova instância da classe App
const expressApp = app.getApp(); // Obtém a aplicação Express do objeto App

// Inicia o servidor Express
//expressApp.listen(3000, () => {
  //console.log('Servidor iniciado na porta 3000');
//});
